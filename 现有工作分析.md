我会按“直接做 encoder/decoder 不兼容或 adapter 的工作”和“相邻可借鉴的 CSI feedback 迁移/微调/多任务工作”两层来查；这类方向可能没有完全同名论文，需要区分直接证据和可迁移思路。

有，但**我没有看到完全等价于“固定某个 seed 的 decoder，然后在不同 seed 的 encoder 码字后面接一个 codeword-space adapter”的公开论文**。最接近的是下面几类工作：它们都在处理 **encoder/decoder 不匹配、共享 decoder、多 UE encoder、adapter/fine-tuning、模型互操作性** 这些问题。

## 1. 最接近：多个 encoder 共享一个 decoder 的互操作工作

最相关的是 **“Demonstrating Interoperable Channel State Feedback Compression with Machine Learning”**。这篇工作讨论了 ML-based CSI feedback 里 UE 侧 encoder 和 gNB 侧 decoder 的兼容性问题，并提出了两种训练路线：一种是每个 UE encoder 配一个 dedicated decoder，另一种是训练一个 **common decoder** 来支持多个 UE encoder。文中明确提到，为了避免 gNB 为每个 UE 厂商/encoder 存一套 decoder，他们训练了一个单一权重的 decoder，能够解码来自多个 UE encoder 的 latent vectors，并通过额外的 index 标识 latent 来自哪个 encoder/dataset。其实这和你的 adapter bank 思路非常接近，只不过它是 **改 decoder / common decoder**，不是在固定 decoder 前面插 adapter。

这篇还做了 PoC 和 OTA 测试，结论里写到确认了 independently developed ML encoders and decoders 的互操作性，并证明一个 gNB decoder 可以支持多个 UE encoders。这个非常适合作为你工作的核心 related work。

你的方案可以说是它的一个替代设计：

```
已有 common decoder 路线:
E_i → D_common(z_i, encoder_id)

你的 adapter 路线:
E_i → A_i→ref(z_i) → D_ref
```

区别是你**不重训/替换 decoder**，而是把不同 encoder 的码字翻译到固定 `D_ref` 能理解的空间。

## 2. Personalized encoders + shared decoder：Fed-PELAD

还有一篇非常贴近的是 **Fed-PELAD: Communication-Efficient Federated Learning for Massive MIMO CSI Feedback with Personalized Encoders and a LoRA-Adapted Shared Decoder**。它采用 **personalized encoders + shared decoder** 架构：每个 UE 本地训练自己的 encoder，BS 侧维护 shared decoder，并用 LoRA 做低秩适配；论文摘要里说只上传 compact LoRA adapter 参数而不是完整模型更新，仿真中通信开销降到传统方法的 42.97%，并在异构条件下有 1.2 dB 的 CSI feedback accuracy 提升。

它和你的区别是：

```
Fed-PELAD:
不同 UE encoder + LoRA-adapted shared decoder

你的想法:
不同 seed encoder + codeword adapter + frozen reference decoder
```

所以 Fed-PELAD 是“共享 decoder + 个性化 encoder + adapter/LoRA”的强相关工作，但 adapter 主要作用在 decoder 参数更新上，而不是显式做 `z_i → z_ref` 的码字空间翻译。

## 3. 多用户、多 encoder 模型：Multi-Task Learning for Multi-User CSI Feedback

**Multi-Task Learning for Multi-User CSI Feedback** 也很相关。它指出传统 DL-based CSI feedback 多数是单用户、单信道场景训练的 autoencoder，在多用户场景下会遇到 **varying channels and varying encoder models across users** 的问题，然后用 multi-task learning 联合训练现有模型。它还报告了 CSINet 与 STNet 联训时性能提升的例子。

这篇没有做你说的 adapter，但它明确把“不同用户有不同 encoder model”作为问题定义。你可以把它作为动机来源之一，然后强调你的方法不是 joint training，而是 **post-hoc latent/codeword alignment**。

## 4. Adapter-based fine-tuning for CSI feedback

还有一个直接带 adapter 关键词的工作：**Adapter-Based Fine-Tuning for Multi-Task Learning Based CSI Feedback in FDD Massive MIMO Systems**。它在 multi-task CSI feedback 的 fine-tuning 中加入 adapter；摘要里提到 Adapter 1 由卷积层、channel weights 和 attention 组成，平均 NMSE 提升约 0.4 dB；另外两个 adapter 方案减少了 BS 侧需要保留的参数量。

这篇对你有两点启发：

```
1. CSI feedback 里使用 adapter 是合理且已有先例的。
2. adapter 可以作为轻量参数模块，减少 BS 侧存储或微调成本。
```

但它不是在不同 seed encoder 的码字之间做翻译，更偏向 **decoder/fine-tuning adapter**。

## 5. CSI-TransNet / plug-in translation module

**Deep Learning for Efficient CSI Feedback in Massive MIMO: Adapting to New Environments and Small Datasets** 里的 CSI-TransNet 也值得看。它提出了 scenario-adaptive 的 plug-in translation module，用有限新场景数据帮助预训练模型适应新环境。论文中结果表明，直接复用预训练模型在新场景下会明显变差，而 plug-in translation module 和数据增强能显著改善性能。

这和你的想法在“translation module”层面很像，但它主要是 **CSI-to-CSI / scenario translation**，不是 **encoder-codeword-to-reference-codeword translation**。不过你可以借它的表述方式，例如：

```
plug-in codeword translation module
latent-space translation
reference-decoder reuse
```

## 6. One-sided / decoder-only CSI feedback：绕开 learned encoder 互操作问题

还有一类工作不是解决 learned encoder 之间的不兼容，而是**绕开这个问题**。比如 **CSI-PPPNet** 使用 UE 侧线性投影压缩 CSI，BS 侧用 deep learning + plug-and-play priors 重构；论文强调 one-sided one-for-all framework 可以减少模型存储、减轻联合训练和模型下发负担，并适合 UE 存储/算力受限的场景。

类似地，**Generative Decoding of Compressed CSI for MIMO Precoding Design** 使用标准化 encoder，例如 3GPP Type I/II codebooks，在 BS 侧用 generative decoder 精修 CSI；作者明确把 encoder-decoder learned models 的 interoperability、backward compatibility 和数据收集开销作为问题，并提出 decoder-only 方案来解决。

这些工作和你的方案方向不同，但可以作为对比：

```
标准化 encoder 路线:
standard encoder + learned decoder

你的路线:
learned heterogeneous encoders + adapter + fixed learned decoder
```

## 你这个方向的潜在 novelty

我建议你把贡献点写成：

```
Codeword-space adapter for decoder-preserving interoperability
```

或者：

```
Latent-space alignment for independently trained CSI feedback encoders
```

比较准确的 novelty 是：

```
已有工作：
1. 训练 common decoder 支持多个 encoder；
2. 个性化 encoder + shared/LoRA decoder；
3. fine-tuning encoder 或 decoder；
4. one-sided / decoder-only 架构绕开 learned encoder 互操作问题。

你的工作：
固定一个 reference decoder，不修改 decoder 权重，
只在不同 encoder 输出码字后添加轻量 adapter，
将 z_i 映射到 z_ref-like latent space。
```

实验上你最好和这几类 baseline 对比：

```
E_i + D_i                         matched baseline
E_i + D_ref                       cross-seed mismatch
E_i + A_i→ref + D_ref             your method
E_i + D_common                    common decoder baseline
fine-tune E_i with frozen D_ref   encoder-only fine-tuning baseline
E_ref + D_ref                     reference upper bound
```

一句话：**相关工作已经很多，但“固定 reference decoder + 码字空间 adapter 翻译不同 seed/不同 encoder 的 latent”这个切入点仍然有空间，尤其适合包装成 decoder-preserving、post-hoc、lightweight interoperability 方案。**